/* tslint:disable:no-unused-variable */

/*
import { NO_ERRORS_SCHEMA } from '@angular/core';
import { TestBed, async, ComponentFixture } from '@angular/core/testing';
import { IfisSimulatorComponent } from './ifis-simulator.component';

import { CookieService } from 'angular2-cookie/services/cookies.service';

describe('Component: IfisSimulator', () => {
  let component: IfisSimulatorComponent;
  let fixture: ComponentFixture<IfisSimulatorComponent>;

  beforeEach( async(() => {
      TestBed.configureTestingModule({
        declarations: [IfisSimulatorComponent],
        schemas: [NO_ERRORS_SCHEMA],
        providers: [CookieService]
      });

      TestBed.compileComponents().then(() => {
      fixture = TestBed.createComponent(IfisSimulatorComponent);
      component = fixture.debugElement.componentInstance;
      });
  }));

  it('should create an instance', () => {
    expect(component).toBeTruthy();
  });
});
*/
