export class VesselCardInfo {
  public index: number;
  public totalVessels: number;

  constructor(index: number, totalVessels: number) {
    this.index = index;
    this.totalVessels = totalVessels;
  }
}
